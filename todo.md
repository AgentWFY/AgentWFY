- [ ] Add ability to start and agent from inside the view and another agent
- [ ] When multiple agents is running and I open on of them I will see running count minus 1, so it is not count current agent.
- [ ] When trying to type an agent message while another agent is running, message input loses focus from time to time.
- [ ] When clicking session list button, nothing is happening when another agent is running in background. Same for other buttons in the agent chat. Maybe not releted to running agent, but it happened before.
- [ ] Looks like changed indicator on tab can stay after agent reload the view, maybe it is because update_at is change on the row and it think the row is update or some other reason. It does not happen all the time.
- [ ] Add ability to open tabs for html and url
- [ ] Rename external views
- [ ] Rename components in renderer from tl- style to awfy- style
- [ ] Persistant state for views
- [ ] History version for views
- [ ] More backup setups for data folder
- [ ] Add ability to open new tabs from generated view
- [ ] Add ability to pass some data when opening the tab for generate view
- [ ] Run backup from ui
- [ ] Restore from back from ui
- [ ] Create a way to generate node js scripts similar to views
- [x] Add notifications for when agent finishes work.
- [ ] When agent is thinking for a long time, there is no feedback visible in the ui. It is unclear if it is still working or not. Maybe show some more indication other than just "in progress".
- [ ] Show context size, so use can use compact session or start a new one. Also need to add ability to compact session or use auto compact.
- [ ] Rewrite how tools cals are shown. There is only one tool right now, maybe format it in a better way.
- [ ] Show running agent indicator in active bar on chat icon. 
- [ ] Add hidden tabs, so agents can open them to make some work and user can continue to work with the app.
- [ ] Add agent db checkpoint with ability to create/restore
- [ ] If agent is working and I scrall to the top. I could not scroll to the bottom anymore. Scroll resent to the top while I try to scroll to the bottom.
